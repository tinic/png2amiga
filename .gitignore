build/
build-wasm/
build-asan/
build-tsan/
service/toolchain/dos/djgpp/
.cache/
.claude/
web/node_modules/
web/dist/
*.exe
*.elf
*.adf
*.o
*.d
test_output/
.DS_Store
fantasy.cpp
test.cpp
# Ad-hoc test outputs at the repo root (test.png, test.h, etc.) — these
# are scratch artefacts from manual CLI runs, not shipped fixtures.
/test.*

# Local scratch / debug artifacts (not shipped)
/debug.png
/ega320.png
/ega350_test.c
/EGA350.IMG
/Monitor_*.png
/engine/
# vAmiga emulator snapshot — large (~8 MB) per-developer
*.vasnap
# Shootout harness pulls down third-party converters and writes encode
# outputs; both are reproducible from the scripts so we don't ship them.
/tools/shootout/vendor/
/tools/shootout/output/
# Internal research / design notes — keep local
/docs/*_research.md
.!*
/examples/tile/out/
