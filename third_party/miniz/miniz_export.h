// Static-library stub for miniz_export.h (normally generated by
// GenerateExportHeader when miniz is built as a shared library). We build
// miniz statically with no symbol visibility controls, so these expand to
// nothing.
#ifndef MINIZ_EXPORT_H
#define MINIZ_EXPORT_H
#define MINIZ_EXPORT
#endif
